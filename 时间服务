Flowable 工作流引擎中用于定义任务截止日期的属性，支持三种格式：

‌固定日期‌（如 2025-10-01）
‌相对时间‌（如 PT30M 表示 30 分钟后）
‌变量引用‌（如 ${dateVariable}，需在流程中提前定义变量）

格式说明
‌固定日期‌：直接使用 ISO8601 格式（如 2025-10-01）
‌相对时间‌：使用 PT 前缀（如 PT30M 表示 30 分钟，PT2H 表示 2 小时）

‌变量引用‌：通过 ${变量名} 获取流程变量值（需在流程定义中提前声明该变量）

示例
xml
Copy Code
<userTask id="task1" name="任务1" flowable:dueDate="${taskDeadline}">
此处 ${taskDeadline} 为流程变量，需在流程定义中提前设置该变量的值。